{
  "name": "downloaded-games",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "upload:bucket": "python scripts/upload_root_folders_to_bucket.py",
    "upload:bucket-dev": "python scripts/upload_root_folders_to_bucket.py --dev",
    "upload:bucket:dry": "python scripts/upload_root_folders_to_bucket.py --dry-run",
    "upload:bucket:retry-failed": "python scripts/upload_root_folders_to_bucket.py --retry-failed",
    "offline:build": "python scripts/build_offline_game_zips.py --build-only",
    "offline:build-dev": "python scripts/build_offline_game_zips.py --build-only --dev",
    "offline:upload": "python scripts/build_offline_game_zips.py --upload-only",
    "offline:build-upload": "python scripts/build_offline_game_zips.py --build-upload",
    "offline:build-upload-dev": "python scripts/build_offline_game_zips.py --build-upload --dev",
    "upload:bucket:deps": "python -m pip install google-cloud-storage pyyaml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
